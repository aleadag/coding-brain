name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo check --all-targets

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy --all-targets -- -D warnings

  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --all-targets

  # Workspace refactor (#279) — verify codexctl-core builds and tests
  # standalone, without the binary crate. Guards the
  # codexctl → codexctl-core dependency direction at the workspace level.
  core-standalone:
    name: Core (standalone)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - name: Build codexctl-core standalone
        run: cargo build -p codexctl-core
      - name: Clippy codexctl-core standalone
        run: cargo clippy -p codexctl-core --all-targets -- -D warnings
      - name: Test codexctl-core standalone
        run: cargo test -p codexctl-core

  # Same guarantee for codexctl-tui (#275): builds and tests without the
  # binary crate. Catches accidental `crate::brain::*` or other binary refs
  # that would only break under workspace builds.
  tui-standalone:
    name: TUI (standalone)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - name: Build codexctl-tui standalone
        run: cargo build -p codexctl-tui
      - name: Clippy codexctl-tui standalone
        run: cargo clippy -p codexctl-tui --all-targets -- -D warnings
      - name: Test codexctl-tui standalone
        run: cargo test -p codexctl-tui

  # Layering check: nothing inside codexctl-core may reference binary-crate
  # modules. This is a textual guard on top of the standalone build above —
  # the build would already fail on a real upward dep, but grep catches
  # subtler attempts (e.g. cfg-gated paths that don't trip on this matrix).
  layering:
    name: Layering
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: No binary modules inside codexctl-core
        run: |
          set -e
          PATTERN='crate::(brain|init|launch|hooks|recorder|session_recorder|app|ui|demo)\b'
          if grep -rEn "${PATTERN}" crates/codexctl-core/src/ ; then
            echo "::error::codexctl-core must not depend on binary-crate modules. See #279 dependency rule."
            exit 1
          fi
          echo "codexctl-core has no upward dependencies on the binary crate."
