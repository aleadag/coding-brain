[package]
name = "previous-compatible-reader"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
rusqlite = { version = "=0.40.1", default-features = false, features = ["bundled"] }
